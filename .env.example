# Optional environment variable overrides.
# Recommended: keep real keys here locally, do not commit .env

GPU_IDLE_NOTIFIER_SEND_KEY=
GPU_IDLE_NOTIFIER_FEISHU_WEBHOOK=
# Optional: point to an explicit config path
# GPU_IDLE_NOTIFIER_CONFIG=/home/you/gpu-idle-notifier/config.json

# Optional runtime overrides for scripts/start.sh
# PYTHON_BIN=/home/you/gpu-idle-notifier/.venv/bin/python
# CONFIG_PATH=/home/you/gpu-idle-notifier/config.json
# RUNTIME_DIR=/home/you/gpu-idle-notifier/runtime

